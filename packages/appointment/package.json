{
  "name": "appointment",
  "version": "1.0.0",
  "description": "Appointment service for the appointment-management monorepo",
  "main": "dist/adapters/lambdas/create-appointment-handler.js",
  "scripts": {
    "build": "tsc",
    "deploy": "npm run build && serverless deploy",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@middy/core": "^6.1.5",
    "aws-sdk": "^2.814.0"
  },
  "devDependencies": {
    "@aws-sdk/client-sns": "^3.758.0",
    "@aws-sdk/client-dynamodb": "^3.767.0",
    "@aws-sdk/lib-dynamodb": "^3.767.0",
    "@types/aws-lambda": "^8.10.92",
    "@types/node": "^16.0.0",
    "esbuild": "^0.25.1",
    "esbuild-plugin-env": "^1.1.1",
    "serverless": "^4.9.0",
    "typescript": "^4.0.0"
  }
}
